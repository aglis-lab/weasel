fun main() {
    // Declaration of lambda
    let a fun()

    // Define of lambda
    a = fun() {
        // Do Something
    }

    // Define of lambda with capture value
    let z ?Car
    let a = fun<weak z>() {
        z.Engine()

        // Do another thing
    }
}
